name: Pull Request Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.6.3
        with:
          token: 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
          owner: test-branching
          base: main
          head: main
